'use strict';

describe('myApp.fccClock module', function() {

  beforeEach(module('myApp.fccClock'));

  describe('fccClock controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var fccClockCtrl = $controller('FccClockCtrl');
      expect(fccClockCtrl).toBeDefined();
    }));

  });
});